import { Injectable } from '@angular/core';
import { UserLogin, Channel, Post, Like, Comment } from '../models'

@Injectable({
    providedIn: 'root',
  })
export class NotificationService {

  constructor() { }

}